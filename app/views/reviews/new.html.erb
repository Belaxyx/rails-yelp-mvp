<h1>Make a review for: <%= @restaurant.name %></h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Create new restaurant", class: "btn btn-primary" %>
<% end %>
